// src/lib/config.ts

// In a Next.js environment, environment variables from .env files are automatically
// loaded by the framework. Explicitly using and calling the 'dotenv' package
// is not necessary and can lead to build errors if the package isn't installed.
// The configuration is handled by Next.js.
